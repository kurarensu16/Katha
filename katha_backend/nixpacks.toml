# Railway Nixpacks configuration for Django (Python)

[phases.setup]
aptPkgs = [
  "default-libmysqlclient-dev",
  "build-essential",
  "pkg-config"
]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install --no-cache-dir -r requirements.txt"
]

# You can add build-time commands here if needed
[phases.build]
cmds = []

[start]
cmd = "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"


